package util;

from math import sqrt;

function isPrime(n: int): bool {
	if (n <= 1)
		return false;
		
	var index: int = 2;
	var upperBound: int = sqrt(n) + 1;

	while(index < upperBound) {
		if (n % index == 0) {
			return false;
		}
		++index;
	}

	return true;
}