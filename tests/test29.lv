const array_length: i32 = 6;
const array: i32[] = [1, 2, 4, 8, 16, 32];

function main(): i32 {
	var sum: i32 = 0;
	var index: i32 = 0;

	while (index < array_length) {
		sum += array[index];
		++index;
	}

	return sum;
}