
function sqrt(n: i32): i32 {
	if (n < 0)
		return -1;
	if (n == 0)
		return 0;

	var x: i32 = n;
	var y: i32 = 1;
	while (x > y) {
		x = (x + y) / 2;
		y = n / x;
	}
	return x;
}

function main(): i32 {
	return sqrt(144);
}