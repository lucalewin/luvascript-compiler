from io import print_number;

function main(): int {
	var index: int = 0;
	var sum: int = 0;
	while (index < 1000) {
		if (index % 3 == 0 || index % 5 == 0) {
			sum += index;
		}
		++index;
	}
	print_number(sum);
	return 0;
}